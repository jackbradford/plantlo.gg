export default function test(

    state = {
        
    },
    action
){

    switch (action.type) {
        case 1:
        case 2:
            return state;
        default:
            return state;
    }
}

